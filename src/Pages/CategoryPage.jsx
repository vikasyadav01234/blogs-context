import React from 'react'
import Header from '../components/Header'

const CategoryPage = () => {
  return (
    <div>
        <Header/>
        <div>
            <button
            onClick={() => }
        </div>
    </div>
  )
}

export default CategoryPage