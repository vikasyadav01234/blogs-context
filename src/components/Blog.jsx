import React from "react";
import {AppContext} from '../context/AppContext'

const Blogs = () => {
    //cansume

    const {loading} =
    return (
        <div>

        </div>
    )
}
export default Blogs